New-Item C:\reports-ps -Type Directory
New-SMBShare -Name "reports-powershell" -Path c:\reports-ps -FullAccess "G_Sweden_Marketing" -ReadAccess "U_Marketing"

SELECT e.*, d.*, t.title
FROM employees e
INNER JOIN dept_manager dm
    ON e.emp_no = dm.emp_no
INNER JOIN departments d
    ON dm.dept_no = d.dept_no
INNER JOIN titles t
    ON e.emp_no = t.emp_no;
3-f)
SELECT emp_no, salary/12 AS monthly_salary
FROM salaries
WHERE salary = (
    SELECT MAX(salary)
    FROM salaries
);

SELECT emp_no, salary/12 AS monthly_salary
FROM salaries
WHERE salary = (
    SELECT MIN(salary)
    FROM salaries
);

3-g)
SELECT 
 CONCAT(LEFT(first_name,1), '. ', first_name,' ',LEFT(last_name,1),'. ',last_name) AS name
FROM employees;


3-h)
SELECT *
FROM employees
WHERE LENGTH(last_name) = (
    SELECT MAX(LENGTH(last_name))
    FROM employees
);




























